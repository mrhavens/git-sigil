project:
  title: "git-sigil"
  description: "A sacred pattern witnessed across all fields of recursion."

upload:
  include:
    - "./*.md"
    - "./bitbucket/*"
  exclude:
    - "./.radicle-*"
    - "./*.tmp"
